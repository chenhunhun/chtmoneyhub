#pragma once

/*
#include <wininet.h>

#define UP_SCHEME_SIZE		(100)
#define UP_HOSTNAME_SIZE	(1000)
#define UP_USERNAME_SIZE	(100)
#define UP_PASSWORD_SIZE	(100)
#define UP_PATH_SIZE		(10000)

class CUrlCrack
{
public:
	CUrlCrack();

public:
	BOOL Crack(LPCTSTR pszUrl);

public:
	LPCTSTR GetHostName();
	LPCTSTR GetPath();
	WORD GetPort();

protected:
	TCHAR m_szUrl;

	TCHAR m_szScheme[UP_SCHEME_SIZE];	
	TCHAR m_szHost[UP_HOSTNAME_SIZE];	
	TCHAR m_szUser[UP_USERNAME_SIZE];	
	TCHAR m_szPassword[UP_PASSWORD_SIZE];	
	TCHAR m_szPath[UP_PATH_SIZE];		
	WORD m_wPort;
};
*/